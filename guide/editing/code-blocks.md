---
title: 代码块
description: 插入带语言高亮的代码块，使用 CodeMirror 编辑和复制代码。
---

# 代码块

<span class="version-badge">适用于 HorseMD v0.13.13</span>

## 插入代码块

输入三个反引号，或通过斜杠菜单搜索“代码”。输入 `/javascript`、`/python`、`/json` 等语言名称，可以直接插入已设置语言的代码块。

代码块使用 CodeMirror 编辑器，提供语法高亮和独立选区。按 Tab 会在当前光标位置插入制表符；Shift+Tab 保持代码块缩进操作。

代码块左侧显示**行号**：行号列紧贴代码块左边缘、与代码行等高，右侧有分隔竖线，行号不可选中。导出 PDF 时代码块也会带上同样的行号。

## 复制与编辑

代码块右上角“复制”按钮会通过系统剪贴板复制整个代码块的纯代码，包括换行和空行，并显示绿色完成反馈和“已复制”提示。即使鼠标停在代码块空白处，复制的也是代码块文档内容，不依赖当前文字选区。超过 100 行时，CodeMirror 为了性能只渲染当前屏幕附近的行，但 HorseMD 仍会复制从第一行到最后一行的完整代码。

如果只需要一部分代码，请在代码块中拖选或按 Shift 扩展选区后使用 `Ctrl/Cmd+C`；部分复制只包含实际选中的范围，不会因为“复制整个代码块”按钮而扩大。导出 PDF 或富文本复制时，编辑器按钮和控制栏不会进入输出内容；普通代码块会保持原样，不会被误当作数学公式渲染。

## 稳定滚动

HorseMD 会在文档解析时直接创建代码编辑器，避免滚到代码块附近后再挂载造成高度变化。选择代码或停留在代码块附近时，页面不应突然跳动。

::: tip Mermaid 是特殊代码块
语言为 `mermaid` 的代码块默认显示图表预览；语言为普通编程语言时显示代码。参见[公式与 Mermaid](/editing/math-and-mermaid)。
:::
