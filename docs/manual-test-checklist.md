# 功能清单 + 手动测试清单(重构回归基线)

> 重构的安全网。每条 = 一个可手动复现的步骤 → 预期。重构每抽一块,跑相关
> 条目;全部重构完后做整表回归。勾选框 `[ ]` 供手动执行时打勾。
> 平台:macOS arm64(`/Applications/HorseMD.app`)为主,Windows/Linux/移动在分节标注。

---

## A. 标签页 / 窗口
- [ ] 双击文件树多个 md → 各开一个标签,只有一个窗口
- [ ] 切换标签 → 编辑器跟随,已挂载的编辑器不重建(秒切)
- [ ] 关闭有未保存内容的标签 → 弹确认;关闭无内容的 → 直接关
- [ ] 关闭最后一个文件 → 进 Home,大纲清空(#20)
- [ ] 拖拽标签?/ 右键「在分屏打开」→ 右格出现,两格都可编辑
- [ ] macOS 关窗口(红绿灯)→ 进程保留;Cmd+Q 有未保存 → 弹确认
- [ ] Windows:最小/最大/关闭按钮工作;最大化图标状态同步
- [ ] Linux:GTK 风格最小/最大/还原/关闭按钮工作;拖动顶栏、双击最大化正常;关闭脏文档弹确认

## B. 文件树 / workspace
- [ ] 打开文件夹 → 侧边栏树展开;增删改文件实时刷新(watcher)
- [ ] 外部改了已打开文件 → 编辑器提示重载(文件监听回显)
- [ ] 右键文件:重命名 / 删除 / 在 Finder 显示 / 导出 PDF
- [ ] 深层目录可展开/折叠;文件多的大文件夹不卡死(性能项)
- [ ] **单一工作区 + 多根**:侧边栏顶部显示「工作区」;「打开文件夹」选另一个文件夹 → 作为第二棵树加入(原文件夹不消失);右键根节点可「从工作区移除」
- [ ] **根节点保护**:根节点可展开/收起/新建子项,但不能拖拽移动、不能重命名/删除真实文件夹;普通子文件/子文件夹仍可拖拽移动、重命名、删除
- [ ] 关窗重开 → 工作区里的多个文件夹根都恢复
- [ ] 旧 session(单 workspace)打开后自动迁移成一个工作区,不丢
- [ ] **飞书式斜杠菜单**:输入 `/` 弹全量;`/h1`/`/H1`/`/标题1`/`/#`→标题1;`/有序`/`/ol`/`/1.`→有序列表;`/无序`/`/ul`/`/-`→无序列表;`/图片`/`/image`/`/img`→图片;`/代码`/`/code`→代码块;`/表格`/`/table`→表格。打字收窄不消失;无匹配时 Enter 正常换行;Esc/退格关闭

## B.1 云同步（桌面）
- [ ] 设置 → 云同步：添加一个已有文件夹，仅创建根目录 `.horsemd/workspace.json`；文件不移动，侧边栏和 watcher 不显示 `.horsemd`
- [ ] 添加 HTTPS WebDAV / S3 连接：错误地址或错误凭据失败；成功后连接列表不显示密码或 Secret；“编辑”可修改公开配置，密码或 Secret 留空仍可通过测试并保留原值
- [ ] 无冲突/删除时点击“双向同步”图标只需一次点击；存在冲突或删除时在当前文件夹下方展开确认预览，且“查看预览”不执行传输
- [ ] 同步预览不使用全屏弹窗；文件清单限制高度并可滚动，关闭图标只收起当前文件夹的预览
- [ ] 云端连接和已连接文件夹的操作为带 tooltip 的图标按钮；同步中显示旋转图标，成功后显示勾选图标
- [ ] “上传本地到云端”：远端同名旧文件和远端额外文件先进入远端 `.horsemd/trash/`，本地文件不被改动
- [ ] “从云端下载到本地”：本地同名旧文件和本地额外文件先移入本地 `.horsemd/trash/`
- [ ] 手工删除远端 `.horsemd/manifest.json` 或清空远端工作区后，“双向同步”显示恢复选择且不生成本地删除计划；选择“上传本地到云端”后远端恢复
- [ ] 空远端首次同步：预览的上传数量和文件数一致；确认后 Markdown、图片、附件均可在第二个隔离 profile 下载
- [ ] 第二个 profile：添加空本地文件夹 → 加入已有云端工作区 → 先看到下载预览 → 确认后内容正确，不产生重复工作区 ID
- [ ] **发布前真实 WebDAV 验收**：使用独立的第三方 HTTPS WebDAV 测试空间（推荐 Koofr），测试连接后上传一个含中文、空格和括号文件名的 Markdown、图片及子目录；第二个隔离 profile 加入同一工作区并下载，逐项核对内容、文件名和附件。测试空间必须与日常/正式同步空间隔离。
- [ ] 两端修改同一个文件：预览显示冲突；同步后原文件和“冲突副本”都存在，任一版本没有静默丢失
- [ ] 一端删除、另一端未修改：预览显示删除；本地和远端 `.horsemd/trash/` 中保留可恢复副本
- [ ] 断网、错误凭据、远端文件在预览后改动：同步停止且显示错误，不推进本地同步状态；重新预览后可恢复

## C. 命令面板 / 快捷键
- [ ] ⌘P → 命令面板,可搜文件 + 命令
- [ ] 选中文字后 ⌘B/Ctrl+B → 加粗且侧边栏不变化;⌘⇧B/Ctrl+Shift+B → 切侧栏;⌘F → 查找;⌘⌥F → 替换(聚焦替换框)
- [ ] ⌘S → 保存;⌘/ → 源码/富文本切换;⌘P 命令齐全(find/replace/theme/source/outline/files)
- [ ] 设置 → 键盘快捷键：搜索 `save`/`保存` 能找到保存命令；显示的默认键位与菜单/命令面板 hint 一致
- [ ] 录制 `保存` 为 ⌘⌥S/Ctrl+Alt+S → 设置页、命令面板 hint 和菜单 accelerator 同步；按新快捷键只保存一次
- [ ] 尝试把 `保存` 录成 ⌘O/Ctrl+O → 显示冲突命令，不写入配置；尝试录成单字母 `A`、Enter、Tab → 显示保留键原因，不写入配置
- [ ] 清空 `切换侧边栏` → ⌘⇧B/Ctrl+Shift+B 不再响应；单项恢复默认后重新响应；全部恢复默认后所有自定义项清空
- [ ] 设置页激活时按 ⌘⇧B/⌘F 或对应 Ctrl 组合 → 不打开侧边栏/查找栏；离开设置页后同一快捷键恢复生效
- [ ] 重启应用 → 自定义快捷键仍存在；清空或恢复默认后的状态也保持
- [ ] 大纲默认保留前两层实际层级：H1→H2→H3 只收起 H3；全 H1 文档不隐藏任何标题
- [ ] 源码/富文本往返后大纲标题、层级和折叠状态不跳变；代码块内的 `#` 不进入大纲
- [ ] 模式切换在前/中/后至少 10 次：可见光标上下文一致且仍可见，纯滚动阅读时视口顶部内容一致
- [ ] 连续链分别覆盖 `源码→富文本→源码→富文本` 和 `富文本→源码→富文本→源码`；表格、列表、图片后方及 fenced code block 内的光标均可逆且可见
- [ ] fenced code block 内分别点击前/中/后行再切到源码，光标落在对应代码行而非统一落到围栏开头；表格单元格内的反引号代码也保持字符位置
- [ ] 源码粗光标为 3px 且略高于文字行；在 100k+ 自动换行文档前/中/后点击时贴合当前行，不遮住无关文字、不出现在空白视口

## D. 源码 ↔ 富文本
- [ ] .md 默认富文本;.txt 默认纯文本(textarea)
- [ ] 富文本末段已有文字时，点击正文下方空白再输入 → 原末段不变，输入出现在新段落；末尾已有空段落时重复点击不产生额外空行
- [ ] ⌘/ 切源码 → 显示 markdown 源码;再切回 → 富文本,内容不丢
- [ ] 使用含连续标题、单个 `~`、紧凑 `-` 列表和列表项末尾 `\\` 的 Markdown：不编辑直接富文本↔源码往返后逐字符一致；在富文本末尾只输入一个字符后，源码仅新增该字符，不新增空行、不转义原有 `~`、不把 `-` 改为 `*`
- [ ] **图片替代文字（#84）**：含 `![测试图片](image/test.png)`、带标题的 `![替代说明](image/test.png "图片标题")` 和旧版尺寸图片的文档，在富文本编辑并连续保存后，普通图片说明不被改为 `1.00`，标题和旧版尺寸图片仍正确
- [ ] 在富文本中全选后粘贴上述原始 Markdown → 保持富文本结构；切源码后仍逐字符保留粘贴内容，不新增空行、不把 `0~9` 改为 `0\\~9`、不把 `-` 改为 `*`
- [ ] 从微信公众号或网页粘贴含标题、加粗、图片的富文本，即使纯文本回退含 `1. ...` 等 Markdown-like 内容，也保留 HTML 的标题层级、行内格式和图片
- [ ] 源码模式编辑 → 保存 → 内容正确(非受控 textarea 同步)
- [ ] A 标签切源码,B 标签仍富文本;切回 A 仍源码(#42)
- [ ] 源码模式编辑后切到 B 标签再切回 A → textarea 内容仍在;切回富文本/保存都正确

## E. 大纲
- [ ] 侧栏切「大纲」→ 列出 H1–H6
- [ ] 大纲顶部总开关：只有 H1→H2 的文档也可点击“全部折叠”，隐藏二级标题；再次点击“全部展开”恢复。H1→H2→H3 文档的首次默认仍保留 H1/H2，不受此开关影响
- [ ] 滚动文档 → 大纲高亮跟随当前标题(reflow-free scrollspy),不卡、停得准
- [ ] 大纲面板软居中:active 项漂到上下边沿 → 自动滚到中间;滚到文档底 → 最后标题也到中间
- [ ] 点大纲项 → 文档跳到该标题
- [ ] **大纲重排（桌面 #82）**：拖同一父级下标题左侧抓手到另一同级标题前/后 → 标题、全部子标题及正文整体移动；切源码确认无关空行、列表符和转义未被重写，再切回富文本确认层级顺序一致。尝试拖到另一父标题下或不同级标题上 → 不允许落下，避免隐式改父子关系
- [ ] 两篇标题不同的文档分屏 → 点击左右编辑区时大纲立即切为对应文档；点大纲只滚动当前焦点栏；左栏源码/右栏富文本时仍正确(#66)
- [ ] 关闭文件 → 大纲清空(#20)

## F. 主题 / 自定义主题
- [ ] 切 light/dark + 6 套内置主题(含莫兰迪)
- [ ] 放 Typora 风格 `.css` 到 userData/themes → 被识别、可应用、相对 url() 生效
- [ ] 切自定义主题后 app chrome 仍是自己的样式,写作区用主题

## G. 国际化
- [ ] 切中文/英文 → 所有 UI 文本、命令面板、斜杠菜单、查找替换、保存提示都跟随

## H. 查找替换(#19)
- [ ] ⌘F → 查找栏(带替换行,默认展开)
- [ ] 输入查找词 → 实时高亮 + `n/m` 计数;▲▼ 上一个/下一个(循环)
- [ ] 输入替换词 →「替换」替当前并跳下一个;「全部」替换全部
- [ ] 替换框 Enter=替一个,Shift+Enter=全部;Esc=关
- [ ] **富文本模式**:替换正确(块内、多匹配)
- [ ] **源码模式(⌘/)**:替换正确(非受控 textarea 适配)← 重点回归
- [ ] **源码模式查找跳转**:当前命中居中且有清晰高亮;连续上下一个均同步更新
- [ ] 查找栏保持打开时富文本↔源码双向切换 → 当前序号、定位和高亮保持
- [ ] 无查找词时替换按钮 disabled

## I. 保存 / 保存提示
- [ ] ⌘S / 浮动保存按钮 → 写盘,桌面端弹「已保存 ✓」(~1.5s 自隐)
- [ ] 保存失败 → 弹红色 sticky 错误
- [ ] 新文件首次保存 → 另存为对话框
- [ ] 有图片的文档首次保存 → 图片搬到 ./assets/,链接改相对路径
- [ ] 外部编辑器保存当前打开文件：干净标签自动刷新；本地有未保存编辑时弹出一次原生提醒，本地内容不被覆盖，提醒说明保存会覆盖外部版本且可“另存为”保留两份
- [ ] 未保存草稿 → 关闭/重启后恢复(session untitled)

## J. 导出 PDF
- [ ] 命令/标签右键/文件树右键导出 PDF → 打开同一个 PDF 导出中心；右侧出现真实分页预览
- [ ] A4/A3/Letter、自定义宽高、纵向/横向、普通/窄/宽/自定义四向边距均会刷新预览
- [ ] 50%–200% 内容缩放会刷新预览，代码块和表头背景色默认保留，快速连续调整后只显示最终设置
- [ ] 分页选择“一级/二级/三级标题前” → 对应标题从新页开始；首个标题不产生空白页
- [ ] 分页选择“分隔符后” → `---` 后换页；“自动分页”不注入强制分页
- [ ] 开启“打印目录页” → 正文前增加目录；H1–H6 深度和正文另起一页开关生效
- [ ] 开启/关闭“PDF 书签大纲” → 阅读器侧栏书签随之出现/消失，不影响是否打印目录页
- [ ] 页眉标题/自定义文字/日期、页脚文字/页码在预览和导出文件中一致
- [ ] 页码范围支持 `1-5, 8, 11-13`；倒序或非法范围显示错误且不能导出
- [ ] 预览缩放和当前页计数正常；远程图片失败/超时显示警告而不是静默缺失
- [ ] 生成 PDF 后富文本样式(标题/代码/图片)正确，页面尺寸与设置一致
- [ ] 段落 LaTeX 公式 `$$...$$` 导出后显示为渲染后的公式且居中，不显示源码、不显示“复制/编辑代码”等公式块控件
- [ ] 未打开的文件右键导出 → 自动打开挂载后再导
- [ ] 大文档首次从文件树导出 → 等待编辑器明确就绪，不因加载超过 3 秒失败
- [ ] 快速连续调整多个选项 → 旧生成任务被取消，最终预览与最后一组设置一致且无残留隐藏窗口
- [ ] 生成中关闭导出中心/关闭窗口 → 任务与临时文件被清理，不继续占用 CPU

## K. 富文本基础(打字即渲染)
- [ ] 标题 H1–H6、有序/无序/任务列表、引用、链接、分隔线、粗/斜/删除线
- [ ] 表格:增删行列、单元格 Enter 换行(往返 `<br>`,#7)、源码往返不坏
- [ ] 宽表格:多列/长文本保持可读列宽,表格内部可左右滑动;编辑区和整个应用不被横向撑开
- [ ] 高且宽的表格:分别在表格顶部/中部/底部、横向滚动条左端/右端悬停并选中行列;行列手柄及操作菜单不被标题栏、编辑区或表格分块遮挡,菜单内每个按钮均可点击
- [ ] 表格单元格边界:悬停后“加行/加列”按钮完整可见且可点击;分别点击后实际增加一行/一列
- [ ] 行内代码:打到反引号后右边自动退出 code(不无限继承,#10)
- [ ] 行内代码:连续输入两个反引号后直接输入 `ab` → 得到行内代码；再输入反引号退出
- [ ] 已有 `` `ab` `` 点击其右边缘并输入 `c` → 变成 `` `abc` ``；点击代码外正文仍输入普通文本(#58)
- [ ] 斜杠 `/` → 块菜单(中文);块拖拽 handle 可用

## L. 代码块(CodeMirror)
- [ ] ``` 代码块 → 语法高亮 + 深色代码面 + Copy 按钮
- [ ] 代码块可编辑;Mermaid/LaTeX 块走预览机制(见 L1/M)
- [ ] **#25 跳页(根因:代码块懒挂载)**:滚到代码块、停留、**选中文字** → 内容**不跳**(eager-mount 修复:`editor-codeblock-eager.js`,代码块打开即挂载、永不卸载,高度恒定);纯滚动停留也不跳(`.editor-scroll` overflow-anchor:auto)。大文档(≥20k,`.hm-cv`)同样验证

## L1. Mermaid
- [ ] ```mermaid → 默认渲染图,工具栏 Hide/Edit 切换源码
- [ ] 粘贴两段 mermaid 进同块 → 自动拆成两块;多个图各自渲染
- [ ] 主题切换 → mermaid 重渲染配色

## M. LaTeX 数学(#18 + 渲染修复 + 打字)
- [ ] `$$x^2+y^2$$` 单行 → 渲染成 **KaTeX 块公式**(不是裸代码块)
- [ ] `$$\n…\n$$` 多行 → 同样块公式
- [ ] 内联 `$E=mc^2$` → 内联 KaTeX
- [ ] **打字** `$$x^2$$`(单行,直接在段落里打)→ 变成块公式(不是行内)
- [ ] **打字** `$E=mc^2$` → 行内公式(单 `$` 不受块规则影响)
- [ ] 先输入 `abc$$`,把光标移到两个 `$` 中间连续输入 `1^2` → 输入不中断、KaTeX 实时预览;光标离开后渲染为 `$1^2$`(#68)
- [ ] 先输入 `$$`,在中间输入纯数字 `123` → 同样实时预览并在离开后渲染(#68)
- [ ] 点击已有行内公式并修改源码 → 编辑框下方预览逐字更新;确认后正文公式与新源码一致(#69)
- [ ] 富文本↔源码往返 → 公式不坏
- [ ] Mermaid 和 LaTeX 同时存在 → 两者都渲染(renderPreview 链式未回退)
- [ ] 输入 `$$` 后回车，连续输入 `x^2 + \sqrt{4}` → CodeMirror 不隐藏、光标不中断、预览同步(#57)
- [ ] `/math` 回车 → 当前段落直接变成 LaTeX 块，光标位于公式源码内而不是下一行(#57)

## N. 高亮 / 行内 HTML(#14)
- [ ] `==text==` → 黄色高亮;工具栏高亮按钮可选红/黄/蓝;⌥⌘H 切黄
- [ ] 行内 HTML `<span style=color:red>x</span>` → 渲染;块 `<table>` HTML → 渲染表格
- [ ] 源码往返保留原始 HTML

## O. Front matter
- [ ] 文档顶部 `---` yaml → 渲染成 key/value 卡片;复杂 yaml → 代码框
- [ ] 文档中间的 `---` → 也识别(不被 mangle 成分隔线+标题)

## P. 粘贴
- [ ] 粘贴 markdown(`#`/表格/引用/```/$$)→ 解析成富文本(智能粘贴)
- [ ] 从微信公众号复制多个 `section`/`div` 段落 → 段落保持分开，加粗/斜体/列表不丢
- [ ] 复制包含编号小标题和懒加载图片的微信公众号长文 → 优先保留 HTML 标题层级、段落、加粗和图片，不误判成 Markdown 列表
- [ ] 粘贴 mermaid 源码 → mermaid 块
- [ ] 粘贴图片 → 写入 ./assets/(已保存文档)或全局粘贴目录(草稿),显示
- [ ] 粘贴进代码块 → 追加代码,不重排
- [ ] 短文档中分别点击正文下方、页面容器下方、左右侧和窗口底部留白并输入 → 光标均落在文档最后；点击滚动条与交互控件不被拦截

## Q. 布局 / StatusBar
- [ ] StatusBar:字号 / 行距 / 段距 / 页宽 滑块 → 实时应用;拖动顺滑(直写 CSS var)
- [ ] 块类型切换(段落/H1…);源码切换;图床按钮
- [ ] 统计:字数/字符/行数 实时更新

## R. 图床
- [ ] 配置图床命令 → 选中图片上传 → 返回 URL 插入
- [ ] 空命令 → 不拦截粘贴(无死 blob:)

## S. review 批注/修订(PR #21 + 卡片 UI 改造)
- [ ] 工具栏「审阅标记」→ 新增/删除/替换/高亮+评论 四种
- [ ] `{++add++}`/`{--del--}`/`{~~old~>new~~}`/`{==hl==}{>>c<<}` 富文本渲染
- [ ] **序号按钮**:每个高亮评论右侧有序号(全局递增 1,2,3…)
- [ ] **同行多个高亮 → 右侧叠放(stack)**:多个序号收到右侧一个 stack,收叠时重叠 ~57%;**悬浮展开**(JS class 驱动,不是 CSS :hover → 不抖动);展开后**完全分离、无重叠**
- [ ] **点击序号** → 打开该条备注卡片(卡片头显示 X/Y,如「2 / 5」)
- [ ] **卡片内容**:高亮文本(小、淡)+ 评论(accent 色块,视觉主体)
- [ ] **卡片按钮**:图标按钮(✏️编辑/✓完成/✕删除/📋复制标记/✨复制AI/📄复制段落)+ tooltip
- [ ] **卡片导航 ‹ ›**:多条备注间切换(切 openGroupKey + 滚动带入视野)
- [ ] **悬浮展开不抖动**:鼠标在展开的序号间移动,稳定不闪烁(JS class + transform)
- [ ] **大文档(≥20k)**:单个高亮序号**实际可见**(不被 `.hm-cv` 的 contain:paint 裁掉)
- [ ] **空评论高亮**仍渲染序号(工具栏插入空评论 → 立刻有序号)
- [ ] 「全部接受/拒绝」→ 各标记正确处理
- [ ] 源码↔富文本往返 → 标记不丢/不坏
- [ ] **中文输入法**(Mac)打替换标记 → 不被 compositionend 破坏(e530fe6)
- [ ] ⚠️ 已知:review 标记在**源码模式**失效(非受控 textarea + updateContent)—— 测时只测富文本模式

## T. 性能(大文档)
- [ ] 打开 ~5万行文档 → 快速打开(textarea 回退);点横幅切富文本 → 分块渐进渲染,不冻
- [ ] 富文本大文档:滚动顺(content-visibility)、打字不卡、大纲跟手
- [ ] >1000 行无空行的文件 → 仍 textarea(安全网);普通长代码块(≤1000 行)→ 富文本(阈值已调)
- [ ] 快速滚动后松手 → 不再追赶(#17,Windows 重点)

## U. 移动端(分平台,Capacitor)
- [ ] iOS/Android:抽屉侧栏、状态栏不重叠(小米/MIUI)
- [ ] 在文档顶部/底部继续上下拖动:只滚动编辑区,整个应用外壳不位移,底部不露空隙
- [ ] 宽 Markdown/HTML 表格可单指左右滑动,横向滑动不带动整个页面
- [ ] 双击/长按选择富文本 → 只显示系统原生菜单，不出现 HorseMD 选区工具栏；复制、粘贴、全选正常
- [ ] 保存到本地文件库 → 弹「已保存 ✓ + 位置」sticky 提示
- [ ] 编辑/查找/主题/大纲 基本可用

## V. 设置页(ActivityBar 齿轮 / 移动端 •••)
- [ ] 左下角齿轮按钮 → 开设置 tab;设置 tab 激活时齿轮高亮;再点齿轮幂等(不开第二个)
- [ ] 设置 tab:EditorArea 隐藏、`<SettingsView>` 显示;切回文档 tab → 编辑器重新可见(不重建)
- [ ] 关设置 tab → 回到原文档 tab
- [ ] **不持久化**:设置 tab 开着关窗口/重启,不恢复(CDP 测过 session LS 无 settings tab)
- [ ] **排版**:字号/行距/段距/页宽滑块拖动 → 上方实时预览 + 切回文档正文同步变(字号修复:正文也变,不只标题)
- [ ] **列表排版（#79）**:设置行距/段距为紧凑、宽松各一次 → 无序列表、有序列表和嵌套列表的行高、项间距与前后留白同步变化；默认比例不变
- [ ] **校对**:拼写开关 关→无红线、开→英文红线;切 tab 一致;重启保持设定值
- [ ] **外观**:主题色块切换生效(亮/暗/莫兰迪/自定义);「打开主题目录」「获取更多主题」可用
- [ ] **语言**:中/英切换,设置页 + 全 UI 文本跟随
- [ ] **图床**:输入命令 → 保存;清空 → 粘贴图片不拦截
- [ ] **键盘快捷键**:可搜索、录制、清空、单项恢复、全部恢复;冲突和保留键提示清晰;固定编辑器命令显示为不可修改
- [ ] **关于**:版本号显示;GitHub/Gitee 链接可点
- [ ] 设置页时:保存 FAB 隐藏、⌘S/导出 不作用于设置页
- [ ] **StatusBar 快捷控件不回归**:排版/主题/语言 仍正常(LayoutControl 抽 AdjustGroup 后)
- [ ] 移动端:••• sheet 有「设置」入口;设置页布局正常(无 activity bar)

## W. Mermaid 全屏灯箱(v0.5.0)
- [ ] 渲染好的 mermaid 流程图 → 点击 → 全屏灯箱弹出(含克隆 SVG)
- [ ] 横向/纵向长图 → 灯箱严格保持原始比例，容器没有固定方形造成的大块空白
- [ ] 缩小/放大按钮与倍率显示同步；适应窗口回到 100%；1:1 恢复原始像素尺寸
- [ ] 灯箱内 Ctrl+滚轮 → 缩放(0.2×–10×);按住拖拽 → 平移
- [ ] Esc / 点背景 → 关闭;拖拽后的 click 不误关
- [ ] 图片灯箱(双击图片)仍正常，小图不被强制放大，长图宽高比不变

## X. 标签拖拽 + 隐藏文件 + 拼写检查(v0.5.0)
- [ ] 拖标签到新位置 → 顺序变 + 重启保持;关闭/右键/中键不回归
- [ ] 设置 → 外观 →「显示隐藏文件」开关 → .claude 等出现;.git/node_modules 仍隐藏
- [ ] 设置 → 校对 → 拼写检查开关 → 开有红线、关无;跨 tab 一致 + 持久化

## Y. 大纲跳转 + 模式切换位置(v0.5.0)
- [ ] 小文档多标题:点最后一条大纲 → ease-out 动画 + 精准落点 + 高亮 = 点击的那条
- [ ] 大文档(≥20k):加载中点大纲 → 加载完再跳(不显示部分标题);落点准
- [ ] 富文本停在某标题 → ⌘/ 切源码 → 仍在该标题附近 → 切回 → 仍准
- [ ] 纯滚动到代码块 + 停下 + 选中文字 → 不跳(eager-mount #25)

## Z. Windows 快捷键(v0.5.0,Windows 实机)
- [ ] 选中文字按 Ctrl+B → 加粗/取消加粗，不切换侧边栏；Ctrl+Shift+B → 只切换侧边栏(#67)
- [ ] Ctrl+W → 只关标签页(不关整个应用);关最后一个标签 → 回 Home 不退出
- [ ] Alt+F4 → 关闭窗口(菜单里 Close 绑定 Alt+F4)
- [ ] macOS:Shift+Cmd+W 关窗口(不变)

## ZL. Linux 安装与桌面集成（Ubuntu/Debian x64 实机）

- [ ] `dpkg-deb --info dist/horse_*_amd64.deb` 成功，包不是 macOS 交叉构建产生的无效小文件
- [ ] `sudo apt install ./horse_*_amd64.deb` 自动补齐依赖并完成安装
- [ ] 应用菜单显示 HorseMD 和正确图标；点击后正常启动且只出现一个实例
- [ ] 文件管理器双击 `.md` / “打开方式 → HorseMD”能打开对应文档
- [ ] 顶栏可拖动；最小化、最大化、还原、关闭及图标状态同步正常
- [ ] 工作区、文件 watcher、Mermaid、图片、源码/富文本切换和 PDF 导出各做一次冒烟测试
- [ ] 终端 `/opt/HorseMD/horse` 可启动；文档不声称存在未安装的 `horsemd` 全局命令
- [ ] `sudo apt remove horse` 可卸载，用户自己的 Markdown 文件不受影响



## AA. 字体设置（#38）

- [ ] 设置 → 编辑器 → 文档字体：点击 → 弹出搜索+列表，完整字体名清晰可读，过长名称有原生提示
- [ ] 悬停字体项 → 右侧预览 + 编辑器正文实时变（不点也变）
- [ ] 点击选中 → 字段显示该字体名（深色，不灰），编辑器生效
- [ ] 选默认 → 清空字体，回默认栈
- [ ] 代码字体同理（代码块字体跟着变，包括 CodeMirror 的 .cm-content）
- [ ] 重启 app → 字体设置保持
- [ ] 列表底部浏览更多字体→ 方正字库（foundertype.com）
- [ ] 代码字体列表底部 → Nerd Fonts（nerdfonts.com）
- [ ] 文档字体和代码字体的搜索框：打开后立即输入，查询不会被异步加载重置，焦点保持在输入框且列表会过滤
- [ ] 设置预览区：文档字体变化 → 预览正文跟着变（不是界面字体）

## AB. 大纲折叠 / 展开

- [ ] 大纲顶部只有一个展开/折叠总开关;图标随全折叠状态切换
- [ ] 当前阅读子标题时,点击父标题箭头 → 父级可折叠,不会无反馈
- [ ] 子标题被折叠后,父标题显示 active/contained 提示;再次展开后子标题恢复显示
- [ ] 源码模式大纲仍可显示、点击跳转、滚动高亮

## AC. 插入附件（#49）

- [ ] 未保存文档执行「文件 → 插入附件…」或命令面板 → 提示先保存
- [ ] 已保存文档选择 PDF/DOCX/ZIP → 同级 `assets/` 出现副本,光标处插入 `[name](<assets/name.ext>)`
- [ ] 源码模式插入附件 → 链接插到 textarea 光标处,切回富文本后仍是链接
- [ ] 富文本模式插入附件 → 链接插到当前光标处,保存后 Markdown 正确
- [ ] 重名附件 → 自动生成 `name-1.ext`,不覆盖旧文件

## AD. Slash 命令菜单边界

- [ ] 顶部段落新起一行输入 `/` → 菜单完整可见,不被顶部栏遮挡
- [ ] 底部段落新起一行输入 `/` → 菜单完整可见,不被底部状态栏遮挡
- [ ] 小窗口/窄窗口底部输入 `/` → 菜单高度收缩,内部可滚动

---

## 重构回归优先级
- **每次必跑**:A(标签)、D(源码/富文本)、H(查找替换,尤其源码模式)、I(保存)、M(LaTeX)、S(review 富文本)、V(设置页)。
- **性能项(T)**:动到 Editor.jsx / scrollspy / 非受控 textarea 后必跑。
- **全量回归**:每个 hook/模块抽完后,整表过一遍。
- **性能项(T)**:动到 Editor.jsx / scrollspy / 非受控 textarea 后必跑。
- **全量回归**:每个 hook/模块抽完后,整表过一遍。
