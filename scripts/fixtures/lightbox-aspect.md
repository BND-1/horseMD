# Lightbox aspect regression

```mermaid
flowchart LR
  A[Start] --> B[Collect content] --> C[Parse Markdown] --> D[Render preview] --> E[Review result] --> F[Publish document] --> G[Done]
```

![Long aspect image](./lightbox-aspect.svg)
